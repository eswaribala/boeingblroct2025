#publish app
dotnet publish MyApp/MyApp.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o publish

1) Install prerequisites (one-time)
.NET SDK (8 or 9)WiX 
v4 SDK (modern, SDK-style)
dotnet new install WixToolset.Sdk
